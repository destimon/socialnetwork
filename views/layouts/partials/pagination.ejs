
<% if (pgs > 0) { %>
  <ul class="pagination text-center">
  	  <li><a href="/contacts/1">Начало</a></li>

  	  <% if (pgs > 1) { %> 
	      <% for(var i = 2; i <= pgs; i++) { %>
	   	    <li><a href="/contacts/<%=i%>"><%=i%></a></li>
	      <% } %>
  	  <% } %> 
  </ul> 
<% } %>